// ─── Game Constants ───────────────────────────────────────────

/** Default sprint target if dynamic calculation isn't applied */
export const WIN_THRESHOLD = 15;

/** Maximum number of rounds before fallback "most cards wins" */
export const MAX_ROUNDS = 15;

/** Minimum players */
export const MIN_PLAYERS = 2;

/** Maximum players */
export const MAX_PLAYERS = 4;

// ─── Feature Flags ────────────────────────────────────────────

/** Phase 2: Advanced Mode with Symmetry Power & Structural Weight stats */
export const ADVANCED_MODE_ENABLED = true;

/** Phase 2: Tessellation Fusion bonus module */
export const TESSELLATION_FUSION_ENABLED = true;

// ─── AI Persona Names ─────────────────────────────────────────
export const AI_BOT_NAMES = [
  'Litema CAD Bot',
  'Tectonic AI',
  'Arch Symmetry Engine',
  'Vernacular Oracle',
];
